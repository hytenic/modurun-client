/* eslint-disable import/prefer-default-export */
export const joinSchedule = (schedule) => {
  const baseUrl = 'willbeadded';
  console.log('일정 참가하는 api랑 연결해야 됨');
  // return axios.post(`${baseUrl}/schedules/user`, {
  //   scheduleId: schedule.id,
  // })
};
